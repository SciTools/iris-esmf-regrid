version: 2

build:
    image: latest

conda:
    environment: requirements/dev.yml

sphinx:
    configuration: doc/source/conf.py
    fail_on_warning: false

python:
    install:
        - method: setuptools
          path: .